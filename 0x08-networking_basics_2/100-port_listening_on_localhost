#!/usr/bin/env bash
# This script sets up a listening service on localhost port 98

PORT=98

# Set up a simple TCP server that listens on port 98 and prints received messages
# Uses netcat (nc), listening on localhost and specified port
nc -lk localhost $PORT
